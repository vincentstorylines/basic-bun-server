{
  "name": "bun-template",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "bundle": "bun run bundler/bundler.ts",
    "list": "bun run samples/list-scripts.tsx",
    "example": "bun run bundle && cd example && bun start && cd ..",
    "fileSample": "bun run samples/file.tsx && cat samples/data/test.json",
    "httpSample": "bun run samples/server.tsx"
  },
  "dependencies": { "baojs": "^0.2.1", "png-to-ico": "^2.1.8", "serve-static-bun": "^0.5.3" },
  "devDependencies": {
    "@types/react": "^18.2.21",
    "bun-types": "latest",
    "react": "^18.2.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}